
import { a as ViewportState, t as Telegram } from "./telegram-DoLvcxLw.js";
import { PropsWithChildren, ReactNode } from "react";
//#region src/components/telegram-provider.d.ts
type TelegramProviderProps = PropsWithChildren<{
  /**
   * Rendered instead of children while the session is being created
   * (during SSR and the first client render).
   */
  fallback?: ReactNode;
  /**
   * Expand the Mini App to the maximum available height after mounting.
   * @default true
   */
  expand?: boolean;
  /**
   * Disable vertical swipes (pull-to-close) after mounting.
   * @default true
   */
  disableVerticalSwipes?: boolean;
}>;
declare const TelegramProvider: ({ children, fallback, expand, disableVerticalSwipes }: TelegramProviderProps) => ReactNode;
//#endregion
//#region src/hooks/use-telegram.d.ts
declare const useTelegram: () => Telegram;
//#endregion
//#region src/hooks/use-viewport.d.ts
declare const useViewportState: () => ViewportState;
declare const useIsFullscreen: () => boolean;
//#endregion
export { TelegramProvider, type TelegramProviderProps, useIsFullscreen, useTelegram, useViewportState };
//# sourceMappingURL=react.d.ts.map