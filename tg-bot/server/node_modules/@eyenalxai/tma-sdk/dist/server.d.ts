
import { t as InitData } from "./init-data-DJ8cL8YF.js";
//#region src/server/validate-init-data.d.ts
type ValidateInitDataOptions = {
  /**
   * Time in seconds during which init data is considered valid since its
   * creation. When 0, expiration is not checked.
   * @default 86400 (1 day)
   */
  expiresIn?: number;
};
/**
 * Validates Telegram Mini App init data using the bot token.
 * @see https://core.telegram.org/bots/webapps#validating-data-received-via-the-mini-app
 * @throws If init data is missing the hash, has an invalid auth date, is expired,
 * or has an invalid signature.
 */
declare const validateInitData: (value: string | URLSearchParams, token: string, options?: ValidateInitDataOptions) => InitData;
declare const isInitDataValid: (value: string | URLSearchParams, token: string, options?: ValidateInitDataOptions) => boolean;
//#endregion
export { type ValidateInitDataOptions, isInitDataValid, validateInitData };
//# sourceMappingURL=server.d.ts.map